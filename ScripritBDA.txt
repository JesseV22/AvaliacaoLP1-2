CREATE TABLE curso (
    id INT PRIMARY KEY,
    nome_cur VARCHAR(50),
    descricao_cur VARCHAR(200)
);


CREATE TABLE aluno (
    id INT PRIMARY KEY,
    nome_al VARCHAR(50),
    email_al VARCHAR(50),
    idade_al INT,
    curso_id INT,
    FOREIGN KEY (curso_id) REFERENCES curso(id)
);


CREATE TABLE professor (
    id INT PRIMARY KEY,
    nome_prof VARCHAR(50),
    email_prof VARCHAR(50),
    especialidade VARCHAR(50),
    curso_id INT,
    FOREIGN KEY (curso_id) REFERENCES curso(id)
);


CREATE TABLE matricula (
    id INT PRIMARY KEY,
    aluno_mar INT,
    curso_mar INT,
    data_mar DATE,
    FOREIGN KEY (aluno_mar) REFERENCES aluno(id),
    FOREIGN KEY (curso_mar) REFERENCES curso(id)
);


Inserir em cada tabela na hora do INSERT INTO your_table (id, name) VALUES (nextval('serial_id') para geeear o id de cada tabela 